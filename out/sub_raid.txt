# precombat
var priority_rotation = priority_rotation
var trinket_sync_slot = 1 if trinket.1.has_use_buff & (!trinket.2.has_use_buff | trinket.1.cooldown.duration >= trinket.2.cooldown.duration)
var trinket_sync_slot = 2 if trinket.2.has_use_buff & (!trinket.1.has_use_buff | trinket.2.cooldown.duration > trinket.1.cooldown.duration)

# actions
var stealth = shadow_dance.up | stealth.up | vanish.up
var haste_trinket_snapshot = (trinket.1.proc.haste.remains <= 1 & trinket.1.proc.haste.up) | (trinket.2.proc.haste.remains <= 1 & trinket.2.proc.haste.up)
var targets = targets.shuriken_storm
var racial_sync = (shadow_blades.up & shadow_dance.up) | fight_remains < 20
var shd_cp = cp <= 2 & 0 | cp >= 6 & 1 | targets >= 5
call_action_list.race
call_action_list.item
call_action_list.cds
shadowstrike if (0 & targets <= 4 | (1 & supercharge_1.up)) & shadow_techniques.stack >= 5 & !ancient_arts.up & !secret_technique.ready
call_action_list.finish if cp >= cp_max_spend - !0
call_action_list.build if stealth | energy > 60
call_action_list.fill if !stealth

# build
shuriken_storm if prev.shadow_dance & premeditation.up & talent.danse_macabre
shadowstrike if !deathstalkers_mark.up & 0 & !0 | targets <= 3 | priority_rotation
shuriken_storm if targets > 1
gloomblade if targets < 2
backstab if targets < 2

# cds
potion if shadow_blades.up | fight_remains < 30
shadow_blades if shd_cp & shadow_dance.ready & secret_technique.ready | fight_remains <= 10
shadow_dance if !stealth & shd_cp & energy >= 30 & ((secret_technique.ready | 0) & (shadow_blades.cooldown >= 9) | (shadow_blades.up & cooldown.secret_technique.duration >= 18)) | fight_remains <= 10
shadow_dance if shadow_blades.up & 1 & haste_trinket_snapshot & secret_technique.cooldown <= 4
vanish if !stealth & energy >= 50 & !subterfuge.up & cp <= 1
shadowmeld if energy >= 50 & !stealth & cp.deficit >= 2

# finish
secret_technique if shadow_dance.up | cooldown.secret_technique.duration < 18 & !shadow_dance.ready
eviscerate if 0
coup_de_grace if secret_technique.cooldown >= 3 | shadow_dance.up
black_powder if targets >= 3 - talent.potent_powder
eviscerate if secret_technique.cooldown >= 3 | shadow_dance.up | shadow_blades.up | 0

# item
use_item,name = light_company_guidon,use_off_gcd = 1 if shadow_blades.up
use_items,slots = trinket1 if (trinket_sync_slot = 1 & (shadow_blades.up | fight_remains <= 20) | (trinket_sync_slot = 2 & (!trinket.2.cooldown.ready & shadow_blades.cooldown > 20)) | !trinket_sync_slot)
use_items,slots = trinket2 if (trinket_sync_slot = 2 & (shadow_blades.up | fight_remains <= 20) | (trinket_sync_slot = 1 & (!trinket.1.cooldown.ready & shadow_blades.cooldown > 20)) | !trinket_sync_slot)

